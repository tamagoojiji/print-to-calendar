# フロントエンド（PWA）ビルド時の環境変数サンプル
# `cp env.sample .env` してから値を入れて `npm run build`。
#
# バックエンド(VPS)の公開URL。末尾スラッシュなし。
# 例: https://ptc-api.example.com
# 未設定だとAPI呼び出しが同一オリジン(github.io)に向き失敗するため、本番ビルド前に必ず設定。
VITE_API_BASE=https://your-backend-host.example.com
